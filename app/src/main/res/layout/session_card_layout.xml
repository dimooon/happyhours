<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    style="@style/FullScreen"
    android:orientation="vertical"
    android:background="@color/kapp2_amber">

    <android.support.v7.widget.CardView xmlns:card_view="http://schemas.android.com/apk/res-auto"
        android:id="@+id/sessions_rad_view"
        style="@style/FullScreen"
        android:layout_gravity="center"
        card_view:cardCornerRadius="4dp"
        android:elevation="100dp"
        android:layout_margin="2dip"
        card_view:contentPadding="4dip"
        android:background="@null">

        <include layout="@layout/session_card_header_layout" />

        <include layout="@layout/view_session"
            android:id="@+id/sessionIncludeLayout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="70dip"
            android:background="@null"/>

    </android.support.v7.widget.CardView>

</LinearLayout>